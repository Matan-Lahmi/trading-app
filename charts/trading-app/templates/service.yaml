apiVersion: v1
kind: Service
metadata:
  name: trading-app-service
spec:
  selector:
    app: trading
  type: ClusterIP
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 80
